# Audio Palette

The template ships with ~400 CC0 sounds (Kenney.nl) organized by category:

| Category | Contents | Count |
|----------|----------|-------|
| `audio/impact/` | glass, metal, wood, bell, punch, soft | 105 |
| `audio/weapon/` | lasers, zaps, knife slices | 21 |
| `audio/footstep/` | carpet, concrete, grass, snow, wood | 35 |
| `audio/powerup/` | power ups, phase jumps, phaser sweeps | 27 |
| `audio/ui/` | clicks, switches, confirms, errors, selects | 151 |
| `audio/rpg/` | books, doors, coins, cloth, metal | 35 |
| `audio/tone/` | blips, peps, stings | 20 |

Root directory also has 7 generated WAVs (hit, death, hurt, pickup, levelup, shoot, ambient).

Browse with `ls static/audio/<category>/` and pick the best fit per event. Reference as e.g. `audio/impact/impactPunch_heavy_000.ogg` in the manifest.

## SFX Audition Tool

Interactive browser tool for auditioning sounds and mapping game events. Lives at `tools/audio-sfx/`.

### Integrated workflow (with a game)

The graybox LLM writes `audio-events.json` in the game root:
```json
{
  "events": {
    "revolver-fire": { "current": "audio/sfx/revolver-fire.ogg", "hint": "sharp crack, western" },
    "hit": { "current": "audio/sfx/hit.ogg", "hint": "meaty punch" }
  }
}
```

Then run:
```bash
npx tsx scripts/pick-audio.ts <slug>
```

This opens the tool pre-filled with the game's events. User auditions, assigns, clicks "Save to game". The script:
1. Copies selected files to `games/<slug>/static/audio/sfx/<event>.<ext>`
2. Writes `games/<slug>/src/audio-manifest.json`

The game imports the manifest:
```typescript
import manifest from './audio-manifest.json'
audio.player.preload(manifest)
```

### Standalone mode

```bash
npx tsx scripts/build.ts audio-sfx --watch --serve
```
Browse sounds, assign to default events, download mapping JSON.

## What the graybox LLM writes

`games/<slug>/audio-events.json`:
```json
{
  "events": {
    "revolver-fire": { "current": "audio/sfx/revolver-fire.ogg", "hint": "sharp crack, six-shooter" },
    "shotgun-fire": { "current": "audio/sfx/shotgun-fire.ogg", "hint": "heavy boom, double barrel" },
    "hit": { "current": "audio/sfx/hit.ogg", "hint": "meaty impact" },
    "enemy_death": { "current": "audio/sfx/enemy_death.ogg", "hint": "satisfying crunch" },
    "player_hurt": { "current": "audio/sfx/player_hurt.ogg", "hint": "sharp pain" },
    "xp_pickup": { "current": "audio/sfx/xp_pickup.ogg", "hint": "light chime" },
    "level_up": { "current": "audio/sfx/level_up.ogg", "hint": "triumphant jingle" },
    "boss_spawn": { "current": "audio/sfx/boss_spawn.ogg", "hint": "ominous rumble" },
    "elite_spawn": { "current": "audio/sfx/elite_spawn.ogg", "hint": "menacing tone" },
    "currency_tick": { "current": "audio/sfx/currency_tick.ogg", "hint": "coin clink" },
    "purchase": { "current": "audio/sfx/purchase.ogg", "hint": "cash register" },
    "shrine": { "current": "audio/sfx/shrine.ogg", "hint": "mystical reveal" }
  }
}
```

`games/<slug>/src/audio-manifest.json` (auto-generated by tool, initially same placeholder values):
```json
{
  "revolver-fire": "audio/sfx/revolver-fire.ogg",
  "hit": "audio/sfx/hit.ogg"
}
```

## Verification

1. `npx tsx scripts/pick-audio.ts` with no args prints usage
2. Create test `audio-events.json`, run script — tool opens with those events pre-filled, hints visible
3. Assign sounds, click "Save to game" — files appear in `static/audio/sfx/`, manifest written
4. Standalone mode still works: `npx tsx scripts/build.ts audio-sfx --watch --serve` — default events + download
